# Normalize line endings: store text as LF in the repo.
* text=auto eol=lf

# Files that MUST be LF (run on Linux / the Pi).
*.sh text eol=lf
*.service text eol=lf

# Binary
*.png binary
*.jpg binary
*.db binary
